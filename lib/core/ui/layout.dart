import 'package:flutter/material.dart';

// Centralized layout constants to improve readability and consistency.

// Breakpoints
const double narrowWidth = 600.0; // Phones stack vertically
const double gridTwoColMinWidth = 900.0; // Dashboard grid switches to 2 columns

// Spacing
const double layoutGap = 16.0; // Standard gap between major areas
const double cardSpacing = 12.0; // Grid item spacing

// Legend sizing
const double legendMaxWidth = 200.0; // Max width for legend side panel

// Common text styles (example; expand as needed)
const TextStyle axisLabelSmall = TextStyle(fontSize: 9);
const TextStyle axisLabelTiny = TextStyle(fontSize: 8);
